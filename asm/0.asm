bits 16

section .text
main:
    mov eax, ebx
    mov eax, [ecx+3]
    mov [ebx+2*ebx+0x12], eax
    mov ax, cs:[bx]
    mov ax, ds:[0x88ff]
    mov ax, ax
    mov bx, bx
    mov ax, bx
    mov ax, cx
    mov ax, [si]
